RewriteEngine On

# Fichiers / dossiers existants (static, img, php/public/static, etc.)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Front controller PHP (accueil + routes)
RewriteRule ^ index.php [L]
